export class Category {
  constructor({ id, name, slug, description, active }) {
    this.id = id;
    this.name = name;
    this.slug = slug;
    this.description = description;
    this.active = active;
  }
}
